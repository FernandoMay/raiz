name: raiz
description: Project Genesis Engine — generate production-ready Flutter projects with Material 3, CI/CD, tests, and brand identity.
version: 1.0.0
repository: https://github.com/btyf/raiz

environment:
  sdk: ^3.11.3

dependencies:
  args: ^2.7.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
